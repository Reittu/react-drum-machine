import React from 'react';
import DrumMachine from './components/DrumMachine';

// Imported as component so we can make dynamic themes and wrap the component with ThemeProvider if we so wish.

function App() {

  return (
      <DrumMachine />
  );
}

export default App;
